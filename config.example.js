/**
 * Configuration Example
 *
 * This file shows how to configure the dashboard.
 * The FRED API key is stored in localStorage via the browser prompt on first visit.
 *
 * To get a FRED API key:
 * 1. Go to https://fred.stlouisfed.org/docs/api/api_key.html
 * 2. Click "Request API Key" (requires free FRED account)
 * 3. Copy your API key
 * 4. Enter it when prompted by the dashboard
 *
 * The key will be stored in your browser's localStorage and used for all FRED API requests.
 */

// No configuration needed in this file - all settings are handled via the browser interface.
// This file exists for documentation purposes.
